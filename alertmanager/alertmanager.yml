global:
  resolve_timeout: 5m

route:
  receiver: local-dev-null
  group_by:
    - alertname
    - severity
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: local-dev-null
